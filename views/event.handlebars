<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css">
    <style>
    #nav {
      width: 100%;
      position: relative;
      list-style-type: none;
      margin: 0;
      padding: 0;
      overflow: hidden;
      background-color: #333;
    }

    #nav li {
      float: left;
    }

    #nav li a {
      display: block;
      color: white;
      text-align: center;
      padding: 14px 16px;
      text-decoration: none;
    }
    #nav li a:hover:not(.active) {
      background-color: #111;
      background-color: #2878d8;
    }

    #nav p1{
      text-align: right;
      right: 0;
      float: right;
      padding: 14px 16px;
      font-size: 15px;
      color: white;
    }

    #events {
        list-style-type: none;
        margin: 0;
        padding: 0;
        width: 100%;
        background-color: #f1f1f1;
        position: relative;
        height: auto;
        overflow: auto;
    }

    #events li a {
        display: block;
        color: #000;
        padding: 10px 0 10px 20px;
        text-decoration: none;
    }

    #nameRight{
        float: right;
        padding-right: 15px;
    }

    #yes{
      background-color: #1fd826;
    }

    #yes a:hover{
      background-color: #6cf071;
    }

    #no{
      background-color: #fc0000;
    }

    #no a:hover{
      background-color: #ff4343;
    }
    </style>
  </head>

  <div id="page">
    <div class="header">
      <a href="#menu"></a>
        Zembo
    </div>
  </div>

    <div id="cont">
    <div id="tp">
      <ul id="nav">
        <li><a href="">+ Post</a></li>
        <li><a href="/group/schedule?classid={{classid}}">+ Event</a></li>
        <p1>{{data.title}} - <i>{{data.name}}</i></p1>
      </ul> 
      <ul id="events">
        <li><a><i id="calendar" class="icon fa fa-calendar fa-1x"></i> <b>{{data.title}}</b> scheduled for {{data.date}} <b><i id="nameRight">{{data.name}}</i></b></a></li>
        <li><a><b>Description - </b><i>{{data.description}}</i></a></li>
        <li><a><b>Where - </b><i>{{data.location}}</i></a></li>
        <li id="yes"><a href="">Yes</a></li>
        <li id="no"><a href="">No</a></li>
      </ul>
    </div>
  </div>

</html>